version: '3.8'
services:
  # bot:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.dc
  #   restart: always
  #   tty: true
  #   stdin_open: true
  #   env_file: project_ctx/.env

  ai_chat:
    build:
      context: .
      dockerfile: Dockerfile.nullai
    restart: always
    tty: true
    stdin_open: true
    env_file: project_ctx/.env